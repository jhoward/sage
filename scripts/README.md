# scripts

`seed_demo_vault.py` writes ~44 interlinked sample notes (AI governance) into the
configured vault, for exercising the features that only mean anything against real
content — related notes, ask-with-context, backlinks, search.

The `part*.py` files are just the note bodies, split for readability.
